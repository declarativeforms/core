version: 1
title: "Turnstile"
description: "Cloudflare Turnstile CAPTCHA challenge. Always required — no validators needed."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      # Turnstile is automatically required and does not need explicit validators.
      - id: question_1
        type: turnstile
        label: "Verify you are human"

    next: done
