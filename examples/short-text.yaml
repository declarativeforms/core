version: 1
title: "Short text"
description: "Single-line text input with length validation."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: short_text
        label: "Full name"
        placeholder: "Jane Smith"
        validators:
          - required
          - type: min_length
            value: 2
            message: "Name must be at least 2 characters."
          - type: max_length
            value: 100
            message: "Name must be at most 100 characters."

    next: done
