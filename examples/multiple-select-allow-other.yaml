version: 1
title: "Multiple select — Allow other"
description: "Multiple select with a free-text 'Other' option."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: multiple_select
        label: "What topics interest you?"
        options:
          - "Technology"
          - "Design"
          - "Business"
          - "Science"
        # Adds a free-text "Other" option for unlisted choices.
        allow_other: true
        validators:
          - required

    next: done
