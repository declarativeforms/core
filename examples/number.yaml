version: 1
title: "Number"
description: "Numeric input with min, max, and pattern validators."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: number
        label: "Age"
        placeholder: "25"
        validators:
          - required
          - type: min
            value: 1
            message: "Age must be at least 1."
          - type: max
            value: 120
            message: "Age must be at most 120."

    next: done
