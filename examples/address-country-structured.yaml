version: 1
title: "Address country — Structured output"
description: "Country autocomplete returning structured components (name, code)."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: address_country
        label: "Country"
        placeholder: "Start typing a country..."
        # Returns structured components: name and country code.
        outputFormat: structured
        validators:
          - required

    next: done
