version: 1
title: "Hidden"
description: "Hidden field for passing metadata (e.g. UTM parameters) without displaying it."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      # Hidden fields are not visible to the respondent.
      # Their value is typically set via URL query parameters.
      - id: utm_source
        type: hidden

      - id: question_1
        type: short_text
        label: "Your name"
        validators:
          - required

    next: done
