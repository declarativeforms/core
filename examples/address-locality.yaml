version: 1
title: "Address locality"
description: "City/locality autocomplete."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: address_locality
        label: "City"
        placeholder: "Start typing a city..."
        # outputFormat can be set to "structured" to return components instead of a string.
        validators:
          - required

    next: done
