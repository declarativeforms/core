version: 1
title: "Email — OTP verification"
description: "Email field with one-time password verification enabled."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: email
        label: "Work email"
        placeholder: "name@company.com"
        # Sends a one-time code to verify the email address before proceeding.
        otp: true
        validators:
          - required

    next: done
