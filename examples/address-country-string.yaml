version: 1
title: "Address country — String output"
description: "Country autocomplete returning the country name as a string."

sections:
  - id: section_1
    title: "Section 1"
    fields:
      - id: question_1
        type: address_country
        label: "Country"
        placeholder: "Start typing a country..."
        # Returns the country name as a single string (e.g. "United States").
        outputFormat: string
        validators:
          - required

    next: done
